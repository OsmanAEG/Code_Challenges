#include <iostream>
#include <vector>
#include <stack>
#include <string>

class DirReduction{
public:
  static std::vector<std::string> dirReduc(std::vector<std::string> &arr);
};

std::vector<std::string> DirReduction::dirReduc(std::vector<std::string> &arr){
  std::stack<std::string> simplified_reversed;

  for(const auto& dir : arr){
    bool cancels  = false;
  }
  return arr;
}

int main(){
  return 0;
}